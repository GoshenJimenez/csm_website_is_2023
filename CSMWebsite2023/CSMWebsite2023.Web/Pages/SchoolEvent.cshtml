@page "/school-events/{id}"
@model CSMWebsite2023.Web.Pages.SchoolEvent
@{
}


@{
    if (Model.SchoolEventItem != null)
    {
        <div class="card" style="width: 18rem;">
            <img class="card-img-top" src="/users/@(Model.SchoolEventItem.UserId).png" alt="Card image cap">
            <div class="card-body">
                <h5 class="card-title">@Model.SchoolEventItem.User.FirstName @Model.SchoolEventItem.User.LastName</h5>
                <p class="card-text">@Html.Raw(Model.SchoolEventItem.Description)</p>
            </div>
            <div class="card-body">
                @{
                    foreach (var medium in Model.SchoolEventItem.SchoolEventMedia)
                    {
                        if (medium.MediaType == Data.Enums.MediaType.ImageUrl)
                        {
                            <img class="card-img" src="@(medium.Value)" alt="">
                        }
                    }
                }
            </div>
        </div>
    }
}